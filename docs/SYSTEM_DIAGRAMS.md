# 📊 Mobile BMR Planogram - System Diagrams

## 1. Activity Diagram - ภาพรวมการทำงานของระบบ

```mermaid
flowchart TD
    Start([เริ่มต้น]) --> Login[Login ด้วยรหัสสาขา]
    Login --> Auth{ตรวจสอบสิทธิ์}
    Auth -->|ไม่ผ่าน| LoginFail[แสดง Error]
    LoginFail --> Login
    Auth -->|ผ่าน| Home[หน้าหลัก Home]
    
    Home --> Menu{เลือกเมนู}
    
    Menu -->|สแกนบาร์โค้ด| Scanner[เปิดกล้อง]
    Menu -->|ดู Planogram| Planogram[แสดง Planogram]
    Menu -->|ประวัติคำขอ| History[แสดงประวัติ]
    Menu -->|ออกจากระบบ| Logout[Logout]
    
    Scanner --> Scan[สแกน/พิมพ์บาร์โค้ด]
    Scan --> Lookup[ค้นหาสินค้า]
    Lookup --> Found{พบสินค้า?}
    Found -->|พบใน POG| ShowFound[แสดงตำแหน่ง]
    Found -->|ไม่พบใน POG| ShowNotFound[แสดงข้อมูลสินค้า]
    ShowFound --> Action{เลือก Action}
    ShowNotFound --> Action
    Action -->|ย้าย| CreateMove[สร้างคำขอย้าย]
    Action -->|ลบ| CreateDelete[สร้างคำขอลบ]
    Action -->|เพิ่ม| CreateAdd[สร้างคำขอเพิ่ม]
    CreateMove --> Submit[ส่งคำขอ]
    CreateDelete --> Submit
    CreateAdd --> Submit
    Submit --> Success[สำเร็จ]
    Success --> Home
    
    Planogram --> SelectShelf[เลือกชั้นวาง]
    SelectShelf --> ViewProducts[ดูสินค้า]
    ViewProducts --> SelectProduct[เลือกสินค้า]
    SelectProduct --> Action
    
    History --> ViewList[ดูรายการคำขอ]
    ViewList --> CanCancel{สถานะ = รอ?}
    CanCancel -->|ใช่| Cancel[ยกเลิกคำขอ]
    CanCancel -->|ไม่| ViewOnly[ดูอย่างเดียว]
    Cancel --> Home
    ViewOnly --> Home
    
    Logout --> Start
```

### คำอธิบาย Activity Diagram

| ขั้นตอน | รายละเอียด |
|---------|-----------|
| **Login** | ผู้ใช้เลือกสาขาและกรอกรหัสผ่านเพื่อเข้าสู่ระบบ |
| **Home** | แสดงเมนูหลัก 4 รายการ: สแกน, Planogram, ประวัติ, รายงาน |
| **Scanner** | ใช้กล้องสแกนหรือพิมพ์บาร์โค้ดเพื่อค้นหาสินค้า |
| **Lookup** | ระบบค้นหาสินค้าใน Planogram และ Master Database |
| **Action** | เลือกการดำเนินการ: เพิ่ม/ย้าย/ลบ ตามเงื่อนไข |
| **Submit** | ส่งคำขอไปยัง Server รอ Admin อนุมัติ |
| **History** | ดูประวัติคำขอทั้งหมด สามารถยกเลิกได้ถ้ายังรอดำเนินการ |

---

## 2. Sequence Diagram - การสแกนบาร์โค้ดและสร้างคำขอ

```mermaid
sequenceDiagram
    autonumber
    participant U as User
    participant App as Mobile App
    participant Cam as Camera
    participant API as BMR Server
    participant DB as Database

    Note over U,DB: กระบวนการสแกนบาร์โค้ด
    
    U->>App: เปิดหน้าสแกน
    App->>Cam: ขอสิทธิ์กล้อง
    Cam-->>App: อนุญาต
    App->>Cam: เปิดกล้อง
    
    U->>Cam: วางบาร์โค้ดในกรอบ
    Cam-->>App: ตรวจจับบาร์โค้ด
    
    App->>App: Throttle Check (500ms)
    App->>App: Validate Length (5-20)
    
    App->>API: GET /lookup/{branch}/{barcode}
    API->>DB: Query Product + Location
    DB-->>API: Product Data
    API-->>App: Response (found/not found)
    
    alt พบใน Planogram
        App->>U: แสดงตำแหน่ง + ปุ่มย้าย/ลบ
    else ไม่พบใน Planogram
        App->>U: แสดงข้อมูลสินค้า + ปุ่มเพิ่ม
    end
    
    Note over U,DB: กระบวนการสร้างคำขอ
    
    U->>App: เลือก Action (เพิ่ม/ย้าย/ลบ)
    U->>App: เลือกตำแหน่งปลายทาง
    U->>App: กดส่งคำขอ
    
    App->>API: POST /pog-requests
    API->>DB: Insert Request (status: pending)
    DB-->>API: Success
    API-->>App: Created
    
    App->>U: แสดง Popup สำเร็จ
    U->>App: กดตกลง
    App->>U: กลับหน้าหลัก
```

### คำอธิบาย Sequence Diagram

| ลำดับ | Component | Action | รายละเอียด |
|-------|-----------|--------|-----------|
| 1-4 | User → Camera | ขอสิทธิ์กล้อง | ตรวจสอบและขอ Permission |
| 5-6 | Camera → App | สแกนบาร์โค้ด | ตรวจจับและส่งข้อมูล |
| 7-8 | App | Validation | ป้องกันสแกนซ้ำ/ผิดพลาด |
| 9-12 | App → Server | API Call | ค้นหาสินค้าใน Database |
| 13-14 | App → User | แสดงผล | ตามผลลัพธ์การค้นหา |
| 15-17 | User → App | สร้างคำขอ | กรอกข้อมูลและยืนยัน |
| 18-21 | App → Server | บันทึก | สร้าง Record ใน Database |
| 22-24 | App → User | Success | แจ้งผลและกลับหน้าหลัก |

---

## 3. Sequence Diagram - การดูและยกเลิกประวัติคำขอ

```mermaid
sequenceDiagram
    autonumber
    participant U as User
    participant App as Mobile App
    participant API as BMR Server
    participant DB as Database

    Note over U,DB: โหลดประวัติคำขอ
    
    U->>App: เปิดหน้าประวัติ
    App->>API: GET /pog-requests/{branch}
    API->>DB: Query All Requests
    DB-->>API: Request List
    API-->>App: JSON Response
    App->>U: แสดงรายการ + สถานะ

    Note over U,DB: ยกเลิกคำขอ
    
    U->>App: กดปุ่มยกเลิก
    App->>U: แสดง Modal ยืนยัน
    
    alt ยืนยันยกเลิก
        U->>App: กดยืนยัน
        App->>API: PATCH /pog-requests/{id}/cancel
        API->>DB: Update status = cancelled
        DB-->>API: Success
        API-->>App: OK
        App->>U: อัพเดทสถานะในรายการ
    else ไม่ยกเลิก
        U->>App: กดเก็บไว้
        App->>U: ปิด Modal
    end
```

---

## 4. API Endpoints ที่ใช้งาน

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/auth/branch-login` | Login ด้วยรหัสสาขา |
| `GET` | `/shelves/{branch}` | ดึงข้อมูล Planogram |
| `GET` | `/lookup/{branch}/{barcode}` | ค้นหาสินค้าจากบาร์โค้ด |
| `POST` | `/pog-requests` | สร้างคำขอเปลี่ยนแปลง |
| `GET` | `/pog-requests/{branch}` | ดึงประวัติคำขอ |
| `PATCH` | `/pog-requests/{id}/cancel` | ยกเลิกคำขอ |

---

## 5. สถานะคำขอ (Request Status Flow)

```mermaid
stateDiagram-v2
    [*] --> pending: สร้างคำขอใหม่
    pending --> approved: Admin อนุมัติ
    pending --> rejected: Admin ปฏิเสธ
    pending --> cancelled: User ยกเลิก
    approved --> completed: ดำเนินการเสร็จ
    completed --> [*]
    rejected --> [*]
    cancelled --> [*]
```

| สถานะ | คำอธิบาย | User Action |
|-------|---------|-------------|
| **pending** | รอดำเนินการ | ยกเลิกได้ |
| **rejected** | ถูกปฏิเสธ | ดูได้อย่างเดียว |
| **completed** | เสร็จสิ้น | ดูได้อย่างเดียว |
| **cancelled** | ยกเลิกแล้ว | ดูได้อย่างเดียว |
